import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFFFFD60A); // Yellow (iOS style)
  static const Color accent = Color(0xFF007AFF); // Blue
  
  // Background
  static const Color background = Colors.black;
  static const Color backgroundDark = Color(0xFF000000);
  
  // Surface
  static const Color surface = Color(0xFF1C1C1E);
  static const Color surfaceDark = Color(0xFF000000);
  
  // Control Colors
  static const Color controlBackground = Color(0x80000000); // 50% black
  static const Color controlActive = Color(0xFFFFD60A); // Yellow
  static const Color controlInactive = Colors.white;
  
  // Flash Effect
  static const Color flashEffect = Colors.white;
  
  // Focus Box
  static const Color focusBox = Color(0xFFFFD60A); // Yellow
  
  // Error
  static const Color error = Color(0xFFFF3B30);
  
  // Success
  static const Color success = Color(0xFF34C759);
}
